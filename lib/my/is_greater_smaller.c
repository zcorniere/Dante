/*
** EPITECH PROJECT, 2018
** is_greater.c
** File description:
** libmy
*/

int is_smaller(int a, int b)
{
    if (a < b)
        return (a);
    return (b);
}

int is_greater(int a, int b)
{
    if (a > b)
        return (a);
    return (b);
}
